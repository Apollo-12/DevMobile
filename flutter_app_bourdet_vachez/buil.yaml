targets:
  $default:
    builders:
      json_serializable:
        options:
          # Options configuration
          explicit_to_json: true
          include_if_null: false
          create_factory: true
          create_to_json: true
      retrofit_generator:
        options:
          nullsafety: true